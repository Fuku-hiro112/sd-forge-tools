# CLAUDE.md — sd-forge-tools プロジェクト規約

このリポジトリで Claude Code が作業する際のルール。

## ドキュメント / 計画の出力形式

- **20 行以上になる計画・設計・提案・調査報告・レビュー資料は、すべて HTML で作成して出力する**
  （テキストの羅列だと読み流して同意してしまう事故を防ぐため。図解・表・タグで一覧性を上げる）。
- HTML は **Claude design** に従い、シンプルで見やすくすること:
  - ライトテーマ（暖色のオフホワイト背景 `#faf9f5` 系 / 白カード / 細いボーダー）
  - アクセントは Claude のコーラル系 `#c96442`
  - 本文は読みやすいサンセリフ、十分な余白、角丸カード、控えめな影
  - 過度な装飾を避け、表・タグ・短い見出しで構造化する
- 配置先は `docs/` 配下（設計・計画は `docs/phase8/`、過去案は `docs/archive/`）。
- 20 行未満の短い回答・要約はチャットのテキストで返してよい。

## 開発ワークフロー

- コード変更（機能追加・バグ修正・リファクタ・UI 変更）は `tdd-forge` skill に従う
  （Pre-flight → RED → GREEN → REFACTOR → VERIFY、L1/L2/L3 検証）。
- **テストケースはユーザーのレビューを経てから GREEN（実装）に進む**。
- テストは `tests/` 配下のみに置く（`scripts/` 直下に `test_*.py` を置くと auto-import で WebUI が起動不能になる）。

## Git 運用

- Git Flow（main / develop / feature/*）。Claude は指定された作業ブランチで作業し、
  main / develop へ直接コミットしない。
- PR はユーザーが明示的に依頼したときのみ作成する。
